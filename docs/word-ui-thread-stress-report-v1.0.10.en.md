# v1.0.10 Word UI-thread Stress Boundary

v1.0.10 only fixes release-installer encoding and failure reporting. It does not change Word selection, drag, resize, rendering, or metadata hot paths. Historical Word UI stress results are therefore not represented as a new performance pass; the v1.0.8 baseline and unresolved boundary remain unchanged.
