# v1.0.9 Word UI-thread Stress Boundary

v1.0.9 only fixes the Office Ribbon version callback and installation acceptance. It does not change Word selection, drag, resize, rendering, or metadata hot paths. Historical Word UI stress results are therefore not represented as a new performance pass; the v1.0.8 baseline and its unresolved boundary remain unchanged.
